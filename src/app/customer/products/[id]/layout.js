export const metadata = {
  title: "Product Page",
  description: "page that show details of one perticular any product",
};

export default function ProductsLayout({ children }) {
  return <section>{children}</section>;
}
