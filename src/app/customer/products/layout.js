export const metadata = {
  title: "explore products",
  description: "page to find the perfect product",
};

export default function ProductsLayout({ children }) {
  return <section>{children}</section>;
}
