export const metadata = {
  title: "Customer Profile Page ",
  description: "profile page dedicated to customers",
};

export default function ProfileLayout({ children }) {
  return <section>{children}</section>;
}
