export const metadata = {
  title: "manage admin | employee",
  description: "page of employee, managers and admins listed",
};

export default function ManagerAdminLayout({ children }) {
  return <section>{children}</section>;
}
