export const metadata = {
  title: "manage departments",
  description: "page to manage departmetns and create new department",
};

export default function AdimnFinanceLayout({ children }) {
  return <section>{children}</section>;
}
