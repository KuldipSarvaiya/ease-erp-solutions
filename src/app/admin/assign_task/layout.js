export const metadata = {
  title: "Assign Task to Managers",
  description: "page for admin to assign task to managers",
};

export default function AssTaskLayout({ children }) {
  return <section>{children}</section>;
}
