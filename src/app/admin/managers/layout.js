export const metadata = {
  title: "manage managers",
  description: "admin page to manage managers",
};

export default function ManagerAdminLayout({ children }) {
  return <section>{children}</section>;
}
