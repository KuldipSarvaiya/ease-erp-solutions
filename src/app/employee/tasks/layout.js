export const metadata = {
  title: "Employee Daily Tasks",
  description: "Page for employee's daily task management",
};

export default function TasksLayout({ children }) {
  return <section>{children}</section>;
}
