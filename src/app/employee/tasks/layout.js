export const metadata = {
  title: "manage task",
  description: "page to manage task of employees itself",
};

export default function TaskLayout({ children }) {
  return <section>{children}</section>;
}
