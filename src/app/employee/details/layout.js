export const metadata = {
  title: "Employee Details Modification Page",
  description: "Page to modify and update the employee details",
};

export default function DetailsLayout({ children }) {
  return <section>{children}</section>;
}
