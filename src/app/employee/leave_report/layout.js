export const metadata = {
  title: "apply for leave",
  description: "page for employees to apply for leave and see leave details",
};

export default function LeaveLayout({ children }) {
  return <section>{children}</section>;
}
