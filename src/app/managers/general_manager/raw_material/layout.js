export const metadata = {
  title: "raw material details",
  description: "raw material details used by this department",
};

export default function LowMaterialLayout({ children }) {
  return <section>{children}</section>;
}
