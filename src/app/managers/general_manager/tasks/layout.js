export const metadata = {
  title: "manage task of manager",
  description: "page to manage past and present task",
};

export default function TaskLayout({ children }) {
  return <section>{children}</section>;
}
