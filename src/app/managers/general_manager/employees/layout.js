export const metadata = {
  title: "employees details",
  description: "details of employees with attendance of this department",
};

export default function EmpLayout({ children }) {
  return <section>{children}</section>;
}
