export const metadata = {
  title: "material stock used by department",
  description: "about the stock of material used by this department",
};

export default function UsedStockLayout({ children }) {
  return <section>{children}</section>;
}
