export const metadata = {
  title: "produced stock of departmenet",
  description: "stock details of raw material or product produced by this department",
};

export default function ProdStockLayout({ children }) {
  return <section>{children}</section>;
}
