export const metadata = {
  title: "manaage revenue",
  description: "page to generate revenue and see revenue history",
};

export default function RevenueLayout({ children }) {
  return <section>{children}</section>;
}
