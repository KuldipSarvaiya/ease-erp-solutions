export const metadata = {
  title: "Finance Expense Reporting",
  description: "page report and view expense history",
};

export default function ExpenceLayout({ children }) {
  return <section>{children}</section>;
}
