export const metadata = {
  title: "Salary Payroll System",
  description: "page to handle payroll of the company",
};

export default function PayrollLayout({ children }) {
  return <section>{children}</section>;
}
