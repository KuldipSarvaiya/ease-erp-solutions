export const metadata = {
  title: "manage task",
  description: "page to manage task of any manager",
};

export default function TaskLayout({ children }) {
  return <section>{children}</section>;
}
