export const metadata = {
  title: "update salary metadata",
  description: "page taht contails for that help to change metadata of salary",
};

export default function Layout({ children }) {
  return <section>{children}</section>;
}
