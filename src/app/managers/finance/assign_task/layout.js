export const metadata = {
  title: "assign task to employees",
  description: "page to assign task to finanace employees",
};

export default function AssTaskLayout({ children }) {
  return <section>{children}</section>;
}
