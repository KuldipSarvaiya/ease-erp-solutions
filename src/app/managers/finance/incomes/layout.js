export const metadata = {
  title: "Finance Income Reporting",
  description: "Income Analysis and reporting download report and add new income record",
};

export default function IncomeLayout({ children }) {
  return <section>{children}</section>;
}
