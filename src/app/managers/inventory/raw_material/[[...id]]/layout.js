export const metadata = {
  title: "manage raw material",
  description: "create and update raw material deetails",
};

export default function RawMaterialLayout({ children }) {
  return <section>{children}</section>;
}
