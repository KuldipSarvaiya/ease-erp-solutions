export const metadata = {
  title: "report raw material order",
  description: "page that contains form to report raw material order",
};

export default function Layout({ children }) {
  return <section>{children}</section>;
}
