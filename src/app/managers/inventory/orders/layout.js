export const metadata = {
  title: "orders management page",
  description: "details of purchase and sales orders , customer and supplier ordes",
};

export default function Layout({ children }) {
  return <section>{children}</section>;
}
