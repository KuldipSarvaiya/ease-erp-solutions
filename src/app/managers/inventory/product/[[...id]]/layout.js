export const metadata = {
  title: "products page",
  description: "page for creating and updating products",
};

export default function Layout({ children }) {
  return <section>{children}</section>;
}
