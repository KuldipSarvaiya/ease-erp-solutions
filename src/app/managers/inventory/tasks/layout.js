export const metadata = {
  title: "Task Management Screen",
  description: "page dedicated to task management",
};

export default function TaskLayout({ children }) {
  return <section>{children}</section>;
}
