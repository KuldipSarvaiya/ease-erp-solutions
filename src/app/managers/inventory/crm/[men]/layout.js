export const metadata = {
  title: "create supplier or customer",
  description: "form to add supplier and customer to system",
};

export default function CrmLayout({ children }) {
  return <section>{children}</section>;
}
