export const metadata = {
  title: "customer relationship management",
  description: "page to manages customers and suppliers",
};

export default function CrmLayout({ children }) {
  return <section>{children}</section>;
}
