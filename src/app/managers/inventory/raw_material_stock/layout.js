export const metadata = {
  title: "raw material stock page",
  description: "page for managing stock and history of raw materials",
};

export default function RawMaterialStockLayout({ children }) {
  return <section>{children}</section>;
}
