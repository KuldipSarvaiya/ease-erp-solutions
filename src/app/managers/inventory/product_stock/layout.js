export const metadata = {
  title: "products stock page",
  description: "functionality to manafe product stock and stock history",
};

export default function ProdStockLayout({ children }) {
  return <section>{children}</section>;
}
