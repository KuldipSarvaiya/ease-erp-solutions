export const metadata = {
  title: "manage employees",
  description: "manage all employees by department group",
};

export default function ManageEmpAdminLayout({ children }) {
  return <section>{children}</section>;
}
