export const metadata = {
  title: "hire new employee",
  description: "create or hire new employee form page",
};

export default function Layout({ children }) {
  return <section>{children}</section>;
}
