export const metadata = {
  title: "Manaage leave reports",
  description: "accept or reject leave applications applied by employees",
};

export default function LeaveLayout({ children }) {
  return <section>{children}</section>;
}
