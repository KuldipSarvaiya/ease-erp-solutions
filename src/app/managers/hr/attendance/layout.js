export const metadata = {
  title: "manage employees attendance",
  description: "managing attendace of all employees department wise",
};

export default function Layout({ children }) {
  return <section>{children}</section>;
}
