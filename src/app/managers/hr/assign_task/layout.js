export const metadata = {
  title: "Asssign Task to Employee",
  description: "assign task to hr department's employee",
};

export default function AssTaskLayout({ children }) {
  return <section>{children}</section>;
}
