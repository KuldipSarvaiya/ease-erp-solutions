export const metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function HolidayLayout({ children }) {
  return <section>{children}</section>;
}
